type Props = {
    accounts: any[];
    setAccounts: any;
    activeAccount: number | null;
    setActiveAccount: any;
};
declare const HbarAccountManager: ({ accounts, setAccounts, activeAccount, setActiveAccount }: Props) => import("react/jsx-runtime").JSX.Element;
export default HbarAccountManager;
